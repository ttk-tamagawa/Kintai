-- ============================================
-- V1: データベース初期設定
-- ============================================
-- タイムゾーン: Asia/Tokyo
-- 対象: 勤怠管理システム共通基盤
-- ============================================

-- タイムゾーンをセッションレベルで設定（Flyway実行時の確認用）
SET timezone = 'Asia/Tokyo';

-- pgcrypto拡張（gen_random_uuid() のフォールバック用）
-- PostgreSQL 14+ では組み込みだが、明示的に有効化しておく
CREATE EXTENSION IF NOT EXISTS pgcrypto;
